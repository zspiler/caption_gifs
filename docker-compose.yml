version: "3.9"
services:
    frontend:
        build: frontend
        ports:
            - "80:80"
        depends_on:
            - api
    api:
        build: api
        ports:
            - "5000:5000"
